@model GPLCC_2.Models.Retard

@{
    ViewData["Title"] = "Create";
}

<h1>Create</h1>

<h4>Retard</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form asp-action="Create">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <div class="form-group">
                <label asp-for="NbJoursRetard" class="control-label"></label>
                <input asp-for="NbJoursRetard" class="form-control" />
                <span asp-validation-for="NbJoursRetard" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="NumPret" class="control-label"></label>
                <input asp-for="NumPret" class="form-control" />
                <span asp-validation-for="NumPret" class="text-danger"></span>
            </div>
            <div class="form-group">
                <input type="submit" value="Create" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-action="Index">Back to List</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
